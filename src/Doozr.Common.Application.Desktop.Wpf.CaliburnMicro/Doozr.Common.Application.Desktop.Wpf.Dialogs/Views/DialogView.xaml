<UserControl x:Class="Doozr.Common.Application.Desktop.Wpf.Dialogs.Views.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="10" >
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<ContentControl Grid.Row="0" x:Name="InnerViewModel" IsTabStop="False"/>
		<DockPanel Grid.Row="1" LastChildFill="False" Margin="0,10,0,0">
			<StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
				<Button Padding="5,3,5,3" Margin="5,0,0,0" Content="OK" x:Name="Ok" IsDefault="True" Style="{StaticResource DialogButton}" />
				<Button Padding="5,3,5,3" Margin="5,0,0,0" Content="Cancel" IsCancel="True" Style="{StaticResource DialogButton}"/>
			</StackPanel>
		</DockPanel>
	</Grid>
</UserControl>
