<UserControl x:Class="Doozr.Common.Application.Desktop.Wpf.Dialogs.Views.ConfirmDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:I18n="clr-namespace:Doozr.Common.I18n.Wpf;assembly=Doozr.Common.I18n.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid Margin="10" >
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Label Grid.Row="0" Content="{Binding Message}" IsTabStop="False"/>
		<DockPanel Grid.Row="1" LastChildFill="False" Margin="0,10,0,0">
			<StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
				<Button Padding="5,3,5,3" Margin="5,0,0,0" Content="{I18n:Translate button_yes}" x:Name="Ok" IsDefault="True" Style="{DynamicResource DialogButton}" />
				<Button Padding="5,3,5,3" Margin="5,0,0,0" Content="{I18n:Translate button_no}" IsCancel="True" Style="{DynamicResource DialogButton}"/>
			</StackPanel>
		</DockPanel>
	</Grid>
</UserControl>
